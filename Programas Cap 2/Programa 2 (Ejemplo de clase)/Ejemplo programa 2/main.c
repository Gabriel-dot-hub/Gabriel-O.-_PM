#include <stdio.h>
#include <stdlib.h>

int P()
{
    float precioED = 0.0; // valor del precio en dolares
    float precioEP = 0.0; // valor del precio en pesos
    const float cambio = 60.0; // valor del peso por dolar
    float precioAP = 0.0; //valor del precio a pagar
    float impuesto = 0.0;

    printf("Calculo impueesto!\n");
    printf("\nIngrese el costo articulo en dolares:");
    scanf ("&f", precioED);
    //0 en informatica es falso
    //cualquier otra cosa es verdadero
    precioEP = precio ED * precioAP
{
    if (precioED > 200.0);

}
}
